---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: python
    tag: 3.6-alpine
inputs:
  - name: pybot
run:
  dir: pybot
  path: sh
  args:
    - -ec
    - |
      pip -q install pytest
      pip -q install -r requirements
      cd bot
      python -m pytest
