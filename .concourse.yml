---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: python
    tag: 3.6-alpine
inputs:
  - name: pybot
run:
  dir: pybot
  path: sh
  args:
    - -exc
    - |
      pip install pytest
      pip install -r requirements
      cd bot
      python -m pytest
